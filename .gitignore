/target
Cargo.lock
.env
.env.*
!.env.example
*.db
.DS_Store

# Local indexes / caches
.codegraph/
.omni/

# AI agent local tooling (Cursor / OpenCode) — keep on disk, do not publish
.cursor/rules/
.cursor/skills/
.opencode/
